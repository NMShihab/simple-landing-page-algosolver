import React from "react";
import styles from "./Header.module.css";
const Header = () => {
  return (
    <div className={styles.nav}>
      <span className={styles.navItem}>Header</span>
    </div>
  );
};

export default Header;
